# 修改日志

## 2026-09-07 L2 ta-only 周/月/季线缺失与进度修复

- **周/月/季线缺失**：`load_kline_for_indicators_by_dates` 取数时放宽到窗口前 90 天，因为周/月/季线的 `trade_time` 是周期起始日，可能早于窗口起点；计算后再按窗口过滤结果，确保每天 4 条（日/周/月/季）。
- **K线数量为 0**：`finish_batch` 不再传入 `kline_count`，避免覆盖 L2 分批写入时通过 `add_kline_progress` 累加的进度值。

## 2026-09-07 L2 ta-only 结果过滤修复

- 修复 ta-only 模式把回看窗口内全部历史指标都写入的问题：现在按 `filter_rows_by_date_range` 过滤计算结果，只保留日期窗口内的行。
- 每只 ETF 每天 4 条（day/week/month/season），10 天 = 40 条，与预期一致。
- `LOOKBACK_BARS=500` 仍作为回看窗口使用（计算基础），不影响写入条数。

## 2026-09-07 L2 指标计算性能与进度优化

- **KDJ 向量化**：逐行 Python 循环改为 `ewm(alpha=1/3, adjust=False)` 等价递推，1636 只 ETF × 36 年 K 线的 KDJ 计算从逐行迭代变为向量化，显著减少耗时。
- **分批 upsert + 实时进度**：`upsert_etf_ta_indicator` 按 `L2_UPSERT_BATCH_SIZE`（默认 2000）分批提交，每批提交后调用 `BatchStatusManager.add_kline_progress` 累加写入 `etl_batch_status.kline_count`，前端跑批状态页可实时看到已插入条数。
- **ta-only 按日期取数**：`--ta-only` 不再走全量批次加载，改为 `load_kline_for_indicators_by_dates` 直接按日期窗口从 `etf_market_kline` 取数，避免把整个 K 线历史一次性读入内存。
- 新增 `sys_param` 默认值 `L2_UPSERT_BATCH_SIZE=2000`，可按机器性能调整（调小更平滑，调大更快）。

## 2026-09-07 L2 计算按钮 NoneType 修复

- 修复 `--ta-only` 模式报错 `'NoneType' object has no attribute 'query_kline'`：模式开关覆盖原本写在 `config.load_from_db()` 之前，会被 sys_param 中的 `ENABLE_*` 配置重新覆盖回 `True`，导致 ta-only 误执行 K 线抽取，而 `market_data` 从未初始化。现在模式覆盖移到 `load_from_db` 之后。
- `--import-only` 的开关覆盖存在同类缺陷，一并修复。
- `get_kline_for_dates` 增加防御性检查：`market_data` 为 None 时抛出明确错误信息，避免再次出现无法定位的 `NoneType` 异常。
- 另提示：sys_param 中 `RUN_MODE` 当前值为 `incrementa`（拼写少一个 `l`），会被脚本回退为 `recent`；如需增量模式请在参数配置中改为 `incremental`。

## 2026-09-07 L3 全历史数据库集合化计算

- 新增 `EtfFiveDimensionResonanceMapper.upsertAllHistory`，全量补算不再由 Java 按交易日循环提交 SQL。
- L3 仅以已有日线 L2 指标行为驱动，避免为没有日线指标的证券创建空结果行。
- 周、月、季指标通过 `LEAD` 形成“当前指标日期至下一指标日期前”的有效区间，日线以区间连接获取目标交易日之前（含当日）的最近周期状态。
- 先对每个周期选定最新有效状态，再同时判断 MACD 金叉状态、红柱和 SAR 多头，修复先过滤合格历史记录再选最新记录导致的历史语义错误。
- `is_yesterday_triggered`、今年首次触发、上次触发和最后触发日期改由窗口函数一次生成，避免对每个目标日期重复扫描完整历史。
- `backfillAll()` 现在仅执行一次 Upsert SQL；按日期范围和刷新最新交易日接口继续保留原有小范围计算路径。

## 2026-08-27 L2 公式标准化

- RSI 改为 Wilder 标准初始化和递推：首个有效值使用前 `n` 个价格变化的平均值；单边上涨 RSI=100，完全不涨不跌 RSI=50。
- MA5/10/20/30/60 改为完整窗口后才输出，避免预热期参与信号。
- KDJ 改为 9 根窗口后开始输出，首个有效 K/D 初始化为 50。
- BOLL 改为完整 20 根窗口后输出，使用 `ddof=0` 总体标准差。
- ATR 改为 14 根真实波幅均值初始化后使用 Wilder 递推；ADX 改为 14 个 DX 均值初始化后使用 Wilder 递推。
- SAR 改为常见两根初始方向、前两根高低价约束和 AF 递推规则，保留 `AF_STEP=0.02`、`AF_MAX=0.20`。
- MACD EMA 递推及 `is_macd_red = MACD > 0` 保持不变。
- 由于 L2 指标公式发生变化，必须重新计算 L2 全量历史数据，然后重新补算 L3 五维共振数据。
- 移除 `FULL_START_DATE` 配置限制；`RUN_MODE=full` 从交易日历最早可用日期开始，L1/L2 时间范围为全量。
- 新增 `POST /api/etf_five_dimension_resonance/backfill-all`，L3 按 L2 中全部日线交易日全量补算。
- L2 技术指标写入改为单个批量事务，避免全量重算时每条记录单独提交事务导致异常缓慢。
- L2 写入增加 `ON DUPLICATE KEY UPDATE`，重复重跑会更新新公式结果，不会因唯一键冲突导致批量事务失效。

### 1. 五维共振策略口径

- 接受业务约定：45 日线暂不单独计算，默认视为“金叉红柱”通过。
- 现有四个周期 `day`、`week`、`month`、`season` 必须同时满足：
  - `is_macd_golden_state = 1`
  - `is_macd_red = 1`
  - `is_sar_bullish = 1`
- 该口径属于策略假设，可能比真正计算 45 日线产生更多触发结果；后续增加 45 日线时需要重新计算历史数据。
- 修正周期对齐：日、周、月、季分别取目标交易日之前（含目标日）最近一条有效指标，不再要求周/月/季的时间戳与日线完全相同。

### 2. 刷新和历史补算改为异步

- `POST /api/etf_five_dimension_resonance/refresh-latest` 现在只提交后台任务并立即返回 `taskId`。
- `POST /api/etf_five_dimension_resonance/backfill` 现在只提交后台任务并立即返回 `taskId`。
- 新增 `GET /api/etf_five_dimension_resonance/tasks/{taskId}` 查询任务状态。
- 前端刷新按钮和历史补算按钮通过轮询任务状态显示成功或失败，不再长时间占用单个 HTTP 请求。
- 同一时间只允许一个五维共振任务执行，避免刷新和历史补算并发写入同一结果表。
- 任务状态暂存在当前后端进程内存中；后端重启后，历史任务状态不可查询，但已经提交到数据库的结果不受影响。
- “银河证券数据导入”原本已经是异步触发，本次保持该模式。

### 3. SQL 修复

- 修复 `qualified_history` 在 MySQL `only_full_group_by` 下的分组错误。
- `SELECT` 和 `GROUP BY` 使用完全一致的 `DATE_FORMAT(DATE(trade_time), '%Y%m%d')` 表达式。
- 共振历史日期基于指标实际 `trade_time`，不再把跨日期补数共用的 ETL 批次号当作交易日期。

### 4. 保持不变的指标定义

- `is_macd_red` 仍表示 `MACD > 0`，不表示 MACD 柱比上一根变长。

### 5. 验证

- 后端：`mvn -q -DskipTests compile` 通过。
- 前端：`npm run build` 通过。
